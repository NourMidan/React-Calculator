import React from "react";
import classes from "./Layout.module.css";

const layout = () => {
  return (
    <div>
      <h1 className={classes.title}>React Calculator</h1>
    </div>
  );
};

export default layout;
